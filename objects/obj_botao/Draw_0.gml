draw_self();

draw_text(x, y, indice);
/*
if (instance_exists(obj_player) or instance_exists(obj_player_espectral))
{
    //Pegando a distancia entre mim e os players
    var _dist = point_distance(x, y, obj_entidade.x, obj_entidade.y);
    //var _dist_2 = point_distance(x, y, obj_player_espectral.x, obj_player_espectral.y);
    
    //draw_circle(x, y, raio, 1)
    //Se a distancia for menor que 30
    if (_dist < raio or _dist < -raio)
    {
        draw_sprite_ext(spr_entra_espectral, 0, x + sprite_width/2, y - 20, 1, 1, image_angle, image_blend, image_alpha);
    }
}
*/